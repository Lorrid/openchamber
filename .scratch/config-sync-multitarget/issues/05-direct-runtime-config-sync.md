# 05 — 直连运行时配置同步

**What to build:** 本地与可直连 OpenChamber 实例之间完成配置探测、备份、流式上传和完成校验，支持双向（推送与拉取），方向切换后重新计算差异；凭据传输受授权通道约束且默认跳过。

**Blocked by:** 02 — 抽出方向无关的同步引擎并迁移 SSH; 04 — 多 SSH 目标、方向选择与白名单组选择.

**Status:** done

- [x] 目标实例提供受授权的配置同步接收协议，覆盖探测、准备、上传和完成校验，并能反向供本地拉取。
- [x] 直连实例可从同步界面被识别、选择和执行，方向选择与白名单组语义与 SSH 目标一致。
- [x] 凭据默认跳过；目标端备份、失败状态和成功收据可被验证。
