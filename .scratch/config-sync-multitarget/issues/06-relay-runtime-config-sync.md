# 06 — Relay 实例配置同步

**What to build:** 已配对的 relay 对端可完成与直连实例相同的双向配置同步流程；凭据传输的授权在配对流程中授予，传输期间维持目标身份与授权边界。

**Blocked by:** 04 — 多 SSH 目标、方向选择与白名单组选择; 05 — 直连运行时配置同步.

**Status:** done

- [x] relay 目标以固定服务身份建立同步通道，执行前刷新可用候选并校验身份。
- [x] 凭据同步授权并入配对信任渠道，未配对授权的对端在任何方向都无法传输凭据。
- [x] 同步请求使用专用门禁和流式上传，保持传输背压与目标隔离。
- [x] 覆盖 relay 重连、身份变化、授权失败和成功完成的集成验证。
