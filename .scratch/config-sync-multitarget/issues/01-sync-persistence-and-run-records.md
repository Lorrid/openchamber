# 01 — 统一同步持久化与运行记录

**What to build:** 现有 SSH 配置同步以目标为边界串行执行，并保存可追踪的运行结果；连接配置和同步状态在并发更新时保持完整。

**Blocked by:** None — can start immediately.

**Status:** done

- [x] 每次同步生成唯一运行标识，并为同一目标提供互斥执行语义。
- [x] 同步结果和恢复信息以独立运行记录保存，保留近期历史供界面与诊断使用。
- [x] 覆盖并发连接配置更新与同步运行记录的自动化验证。
