/**
 * Telegram command reference — derived from the shared messenger catalog.
 * Prefer importing from `messenger-commands-data` for new call sites.
 */
export {
  TELEGRAM_COMMANDS,
  TELEGRAM_COMMAND_CATEGORY_ORDER,
  type TelegramCommandCategory,
  type TelegramCommandEntry,
} from './messenger-commands-data';
