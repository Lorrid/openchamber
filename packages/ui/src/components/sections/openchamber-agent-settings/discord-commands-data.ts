/**
 * Discord command reference — derived from the shared messenger catalog.
 * Prefer importing from `messenger-commands-data` for new call sites.
 */
export {
  DISCORD_COMMANDS,
  DISCORD_COMMAND_CATEGORY_ORDER,
  type DiscordCommandCategory,
  type DiscordCommandEntry,
} from './messenger-commands-data';
